import { add, greet } from '../src';

describe('generator-lerna-typescript module', () => {
  it('should add', () => {
    expect(add(2, 3)).toEqual(5);
  });
  it('should greet', () => {
    expect(greet('world')).toEqual('generator-lerna-typescript says: hello to world');
  });
});
