import { <%= classTypeName %> } from '../src/<%= className %>';

describe('<%= packageName %> <%= className %> class', () => {
  let sut: <%= classTypeName %>;
  beforeEach(()=> {
    sut = new <%= classTypeName %>()
  });
  it('should add', () => {
    expect(sut.add(2, 3)).toEqual(5);
  });
});
