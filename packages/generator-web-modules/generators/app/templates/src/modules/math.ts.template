export function parse(values: string): number[] {
  return values.split(',').map((n) => Number(n));
}
export function adder(...addends: number[]): number {
  return addends.reduce((a, b) => (a += b), 0);
}
